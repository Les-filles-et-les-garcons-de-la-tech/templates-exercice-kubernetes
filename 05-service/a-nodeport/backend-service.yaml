apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: <prenom>  
spec:
  type: ###                  # A remplir
  selector:
    tiers: ###               # A remplir
  ports:
  - port: ###                # port exposé par le service
    targetPort: ###          # port ciblé du pod
    nodePort: _PORT_BACKEND_ # port exposé par TOUS les noeuds du cluster