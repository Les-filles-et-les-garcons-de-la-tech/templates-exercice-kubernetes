apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: _PRENOM_  
spec:
  replicas: 1
  selector:
    matchLabels:
      tiers: ###        # A remplir
  template:
    metadata:
      labels:
        tiers: ###      # A remplir
    spec:
      containers:
      - name: frontend-container
        image: fgtech/frontend-todolist:_STUDENT_
        imagePullPolicy: Always
        ports:
        - containerPort: 81
        env:
        - name: BACK_URL
          value: "http://_IP_NODE_:_PORT_BACKEND_"

