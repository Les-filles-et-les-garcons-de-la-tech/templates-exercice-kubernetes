apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: <prenom>  
spec:
  type: NodePort
  selector:
    tiers: backend
    user: <prenom>
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30081
