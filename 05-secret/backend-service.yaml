apiVersion: v1
kind: Service
metadata:
  name: backend-service-<prenom>
spec:
  type: NodePort
  selector:
    tiers: backend-<prenom>
  ports:
  - port: 80
    targetPort: 80
    nodePort: _PORT_BACKEND_
