apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-pvc
  namespace: _PRENOM_  
spec:
  storageClassName: manual
  selector:
    matchLabels:
      env: formation    
  # Adapter la demande au PV défini