apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: _PRENOM_  
spec:
  replicas: 1
  selector:
    matchLabels:
      tiers: frontend
  template:
    metadata:
      labels:
        tiers: frontend
    spec:
      containers:
      - name: frontend-container
        image: fgtech/frontend-todolist:stu_NUMBER_
        imagePullPolicy: Always
        ports:
        - containerPort: 81
        env:
        - name: BACK_URL
          value: "http://_IP_LB_INGR_CONTR_/_PRENOM_-back"

