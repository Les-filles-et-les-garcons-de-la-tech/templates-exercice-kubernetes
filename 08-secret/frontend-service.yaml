apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: <prenom>  
spec:
  type: NodePort
  selector:
    tiers: frontend
  ports:
  - port: 80
    targetPort: 81
    nodePort: _PORT_BACKEND_
