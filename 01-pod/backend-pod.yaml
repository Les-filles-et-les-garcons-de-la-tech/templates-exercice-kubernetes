apiVersion: v1
kind: Pod
metadata:
  name: mypod
  labels:
    name: mypod
spec:
  containers:
      - name: webapp-container
        image: ###               # A remplir
        imagePullPolicy: Always
        ports:
        - containerPort: ###     # A remplir
        env:
          - name: ###            # A remplir
            value: "#"           # A remplir
